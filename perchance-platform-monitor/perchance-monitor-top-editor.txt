title
  Perchance Platform Monitor
description
  Live backend status, Cloudflare regional health, and model-change detection for perchance.org. Paste the matching HTML into the HTML panel.
tags
  perchance, status, monitor, cloudflare, backend, uptime, region, outage, health, proxy, test, docs

$meta
  header
    mode = minimal

aiTextPlugin      = {import:ai-text-plugin}
textToImagePlugin = {import:text-to-image-plugin}
uploadPlugin      = {import:upload-plugin}
superFetch        = {import:super-fetch-plugin}
kv                = {import:kv-plugin}
commentsPlugin    = {import:comments-plugin}

// Hidden shared store for region telemetry. Each visitor posts ONE coarse
// record per session (Cloudflare colo + country, no IP) so outages can be
// mapped by region. Rendered hidden in the HTML panel via [commentsPlugin(telemetryOptions)].
telemetryOptions
  channel = pmon-telemetry-v1
  width = 100%
  height = 300px
  commentPlaceholderText = automated region telemetry
  submitButtonText = send

// Used by the regression test suite (Test suite tab).
monitorTestList
  alpha
  bravo
  charlie

monitorTestFunc(x) => "echo:" + x

output
  Open the HTML panel and paste the monitor HTML.
