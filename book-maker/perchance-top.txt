$meta
  title = Book-maker - write a whole book, page by page
  description = A storybook builder that actually plans ahead. Pick a genre, theme and tone, bring your own cast (or import AI-Character-Chat characters), set each one's role and fate, then write a book one page at a time - streaming, fully editable, continue from the cursor. Give it an ending and it builds toward it, planting and paying off clues, so even a mystery holds together. It remembers your people, places and lore as the story grows, and can look up real facts - places, words, history, the night sky, mythology - to keep the world believable. Set chapter markers and footnotes, save to a library, export to txt / md / json, and attach the story's memories to pick it up again later. Free, runs in your browser, and your data stays on your device.
  tags = story, book, novel, writer, writing, fiction, ai story, story generator, book maker, mystery, fantasy, sci-fi, romance, horror, roleplay, character, interactive fiction, free

// ---- imports (top editor only; keep this file ASCII - Perchance audits the DSL pane) ----
aiTextPlugin = {import:ai-text-plugin}
// kv-plugin: the saved-book library (My Books). Off-platform it falls back to localStorage.
kv = {import:kv-plugin}
// super-fetch-plugin: powers the Almanac's real-world lookups (Wikipedia, dictionary,
// word-tools, country, on-this-day, quotes, translate) through a CORS proxy.
superFetch = {import:super-fetch-plugin}
// comments-plugin: reader comments, toggled by the pinned button at the top-right of the frame.
commentsPlugin = {import:comments-plugin}
// advanced-hypnosis-narration-engine: pickFromBank - anti-repetition motif rotation with memory.
hne = {import:advanced-hypnosis-narration-engine}
// optional, for cover / scene art later:
// textToImagePlugin = {import:text-to-image-plugin}
// optional, for character-file upload + share links:
// uploadPlugin = {import:upload-plugin}

// The HTML pane is the whole app - self-contained: the editor, the story engine, and the
// offline fallback narrator are all bundled in there (a base64 payload that a small loader
// decodes and runs), so nothing else needs hosting. The HTML pane may use non-ASCII freely
// (it is an iframe); only this top editor must stay ASCII. To rebuild the pane after editing
// the source, run book-maker/build-pane.sh.
